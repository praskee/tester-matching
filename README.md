# Tester Matching
Match testers by experience based on search criteria.

Example input:

Country: `ALL`

Device: `iPhone 5,Nexus 4`

## Build
Run `mvn clean install` to build the project.
## Run
Run `mvn exec:java` to execute the application.
